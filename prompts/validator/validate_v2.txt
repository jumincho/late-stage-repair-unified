VERSION: validator_v2

Return valid JSON only.

Validate the candidate set without using any hidden gold answer.

Filter or flag:
- duplicates by normalized answer
- duplicates by same underlying failure mode
- malformed outputs
- trivial or obviously silly options
- ambiguous or non-actionable options

JSON schema:
{
  "decisions": [
    {
      "option_id": "A",
      "keep": true,
      "reason": "short reason",
      "duplicate_of": null
    }
  ]
}

Question:
{question}

Candidates:
{candidates_json}
